python manage.py shell

from django.contrib.auth.models import User

user = User.objects.create_user('john', 'lennon@thebeatles.com', 'johnpassword')
user.save()



#other notes
#currently running using gunicorn through nginx as a proxy

from manage.py directory, run:

gunicorn HitWalker2.wsgi:application
#or 
nohup gunicorn HitWalker2.wsgi:application &

#nginx is started by
sudo nginx
#stopped or reloaded by:
sudo nginx -s stop
sudo nginx -s reload

The nginx config file is default-nginx here.
#called default in /etc/nginx/sites-available


